
modéliser les implem standards comme des proceseurs switching.
avec une dimension variable, qui est ajoutée au bin-packing du FPGA

dans le tableau des CPProcessseur, les switching sont donc ajoutés à ce tableau là

les implems qui peuvent tourner sur ces processeurs switching spécaliss sont donc uniqument les standards liées à ces processeurs (fictifs)

mais ces processeurs sont visibles par tout le monde (mais beaucoup d'interdiction bien sûr)

problème: les implem de référence ont-elles des champs en plus?

problème: comment noter une implem de référence nas une liste d'implem?

pour l'instant, c'est implementations:List[Implementation]
